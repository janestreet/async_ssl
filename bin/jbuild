(executables
 ((names
   (hello
    ))
  (flags (:standard -safe-string))
  (libraries
   (async
    core
    async_shell
    async_ssl
    ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version 1)
